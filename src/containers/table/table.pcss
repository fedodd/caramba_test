.table {
  font-size: var(--fontSize-table);
  font-weight: 300;
  & thead,
  & caption {
    display: none;
  }
  @media only screen and (min-width: 720px) {
    & thead,
    & caption {
      display: initial;
    }
  }
}

.descr {
  margin: 0;
  padding: 0;
  color: var(--color-text_descr);
  font-size: var(--fontSize-table_descr);
}

.color {
  width: var(--circle-radius);
  height: var(--circle-radius);
  border-radius: 50%;
  border: var(--circle-border);
}
