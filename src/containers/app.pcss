.title {
  font-weight: bold;
  font-size: 42px;
  line-height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 30px 0 17px 0;
}

.main {
  max-width: 960px;
  margin: 0 auto;
}

.holder {
  margin: 0 var(--margin-holder);
}
