.form {
  margin-bottom: 50px;
}

.fieldset {
  padding: 0;
  border: 0;
  margin: 0;
  margin-top: 17px;
}

.legend {
  margin-bottom: 5px;
  /* margin-left: var(--padding-inputText); think they must be on same line */
  width: 100%;
  font-size: var(--fontSize-input);
  line-height: var(--lineHeight-input);
  color: var(--color-text_light);
}
